import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Proyecto Girasol',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
